# Changelog

## 0.1.1

- restored `ChainUseCase` as an inheritable public runtime primitive
- aligned the documented guard API with the actual `Allow` and `Deny` helpers

## 0.1.0

- created the standalone `dotnet-ode` package
- added direct, chain, sequence and guard-oriented runtime primitives
- added typed outputs and lightweight MVVM channels
- documented NuGet publication flow and local validation
